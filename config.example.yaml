# Non-secret app config. Per-user credentials (Gmail App Password, CAS PDF
# password, etc.) are NOT here — those live encrypted in data/app.db, which
# is gitignored and never leaves your machine.

# The single user who is allowed to invite others. After first launch this
# email is the only one that can self-register as admin.
admin_email: gagan.parakh@gmail.com

cams:
  cams_sender: donotreply@camsonline.com
  cams_subject: "CAMS Mailback Request"

gmail:
  imap_host: imap.gmail.com
  imap_port: 993

playwright:
  headless: true                  # set false to watch the CAS form being filled
  slow_mo_ms: 0
